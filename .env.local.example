# Opcional. Sin esto, las notificaciones usan BroadcastChannel (cross-tab, mismo browser).
# Con esto, usan Supabase Realtime Broadcast (cross-device). No se crean tablas.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
